import { View } from 'react-native';

// This file is the entry point for the router.
// The root layout will handle all navigation and authentication logic.
export default function Index() {
  return <View />;
}
